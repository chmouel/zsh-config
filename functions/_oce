#compdef oce
function oce () {
    [[ -z $1 ]] && { echo 'I need a pod'; return 1 ;}
    oc exec -i -t -p $1 -- bash -il
}

_oce () {
    compadd $(oc get pods|awk '$3 ~ /^Running$/ { print $1}')
}

compdef _oce oce
